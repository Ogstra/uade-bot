services:
  uade-go:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      DASHBOARD_ADDR: ":3000"
    ports: ["3000:3000"]
    volumes: ["./data:/app/data"]
    healthcheck:
      test: ["CMD", "/app/healthcheck"]
      interval: 30s
      timeout: 5s
      retries: 3
